name: "Release Binaries (Reusable)"

on:
  workflow_call:
    inputs:
      commit_sha:
        required: true
        type: string
      tag_version:
        required: false
        type: string
      builds:
        description: "Comma-separated list of artifact names to release"
        required: true
        type: string
      release_description:
        description: "Release notes / description (multiline allowed)"
        required: false
        type: string
      repo_dir:
        required: false
        type: string
        default: ${{ github.workspace }}

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:

      # Checkout the repo (needed if scripts rely on repo content)
      - uses: actions/checkout@v6

      # Determine release tag
      - name: Determine release tag
        id: tag
        run: |
          if [ -z "${{ inputs.tag_version }}" ]; then
            echo "ERROR: Commit tag not found"
            exit 1
          else
            TAG="${{ inputs.tag_version }}"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      # Download artifacts listed in inputs.builds into a known folder
      - name: Download and collect install artifacts only
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifacts
          mkdir -p release_files

          IFS=',' read -ra ARTIFACT_NAMES <<< "${{ inputs.builds }}"

          for ARTIFACT in "${ARTIFACT_NAMES[@]}"; do
            ARTIFACT=$(echo "$ARTIFACT" | xargs) # trim whitespace

            echo "Downloading artifact: $ARTIFACT"
            gh run download --name "$ARTIFACT" --dir "artifacts/$ARTIFACT"

            INSTALL_DIR="artifacts/$ARTIFACT/install"

            if [ -d "$INSTALL_DIR" ]; then
              echo "Found install folder in $ARTIFACT"

              # Copy ONLY install contents (preserve structure inside install)
              rsync -a "$INSTALL_DIR/" "release_files/"
            else
              echo "No install folder in $ARTIFACT — skipping"
            fi
          done

          echo "Final release files:"
          find release_files -type f
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Create GitHub release (includes all prepared artifacts)
      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: ${{ steps.tag.outputs.tag }}
          body: ${{ inputs.release_description }}
          files: |
            release_files/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # # Create GitHub release
      # - name: Create release
      #   id: create_release
      #   uses: softprops/action-gh-release@v3
      #   with:
      #     tag_name: ${{ steps.tag.outputs.tag }}
      #     name: ${{ steps.tag.outputs.tag }}
      #     body: ${{ inputs.release_description }}
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # # Upload all artifacts from the builds list
      # - name: Upload artifacts
      #   run: |
      #     IFS=',' read -ra ARTIFACT_NAMES <<< "${{ inputs.builds }}"
      #     for ARTIFACT in "${ARTIFACT_NAMES[@]}"; do
      #       echo "Downloading artifact: $ARTIFACT"
      #       # Download artifact from the same workflow run
      #       gh run download --name "$ARTIFACT" --dir ./tmp

      #       # Assuming each artifact contains a single archive file
      #       FILE=$(ls ./tmp)
      #       echo "Uploading $FILE to release ${{ steps.tag.outputs.tag }}"
      #       gh release upload "${{ steps.tag.outputs.tag }}" ./tmp/"$FILE" --clobber --repo ${{ github.repository }}
      #       rm -rf ./tmp
      #     done
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
